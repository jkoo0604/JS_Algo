class Node {
    constructor(data) {
        this.data = data;
        this.next = null;
    }
}

class slStack {
    constructor() {
        this.head = null;
    }

    // add to top (add new head)
    push(newNode) {
        
    }

    // remove from top, (remove head)
    pop() {
        
    }

    // aka top
    peek() {
        
    }

    // check if empty
    isEmpty() {
        
    }
}